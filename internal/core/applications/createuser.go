package applications

import "net/http"

// CreateUser TODO 1. Get the tenantId from the request context, 2. Create the user in the database, 3. Return the user
func (m *Microservice) CreateUser(wr http.ResponseWriter, r *http.Request) {
	return
}
