package applications

import "net/http"

// UpdateUser TODO 1. Get the tenantId from the request context, 2. Update the user in the database, 3. Return the user
func (m *Microservice) UpdateUser(wr http.ResponseWriter, r *http.Request) {
	return
}
